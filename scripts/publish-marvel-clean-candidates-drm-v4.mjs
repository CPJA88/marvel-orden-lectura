import fs from 'node:fs/promises';
import path from 'node:path';

const root=process.cwd();
const cacheFile=path.join(root,'source','marvel-cache','index.json');
const resultsFile=path.join(root,'artifacts','marvel-not-listed-v4','clean-candidates-drm-v4-results.json');
const summaryFile=path.join(root,'artifacts','marvel-not-listed-v4','clean-candidates-drm-v4-publish-summary.json');
const STATUS={MU:1,NO_DIGITAL:3,NOT_LISTED:4,MU_LINK_MISSING:5};
const DRN_RE=/^drn:src:marvel:unison::prod:[0-9a-f-]{36}$/i;
const str=v=>v==null?'':String(v),now=new Date().toISOString();
const count=(p,s)=>p.entries.filter(r=>Number(r?.[3])===s).length;
function recompute(p){const c1=count(p,1),c5=count(p,5),c3=count(p,3),c4=count(p,4);p.matched=c1+c5;p.verifiedMU=p.matched;p.unavailable=c3;p.noDigital=c3;p.notListed=c4;p.linkReady=p.entries.filter(r=>Number(r[3])===1&&DRN_RE.test(str(r[5]))).length;p.linkMissing=p.entries.filter(r=>Number(r[3])===5||Number(r[3])===1&&!DRN_RE.test(str(r[5]))).length;p.linksPrebuilt=p.linkMissing===0;p.readerFallbackGcdIds=[...new Set((p.readerFallbackGcdIds||[]).map(Number).filter(Number.isFinite))].sort((a,b)=>a-b);p.readerFallbackReady=p.readerFallbackGcdIds.length;p.functionalLinkReady=p.linkReady+p.readerFallbackReady;p.functionalLinkMissing=Math.max(0,p.matched-p.functionalLinkReady);return p}

const[pack,research]=await Promise.all([fs.readFile(cacheFile,'utf8').then(JSON.parse),fs.readFile(resultsFile,'utf8').then(JSON.parse)]);
if(Number(pack.localCount)!==51002||pack.entries?.length!==51002||pack.matched!==29189||pack.noDigital!==1135||pack.notListed!==20678||pack.functionalLinkMissing!==0)throw new Error('Baseline inesperada.');
const s=research?.summary||{};if(research?.version!==4||research?.mode!=='clean-candidates-drm-v4-results'||research?.writesCache!==false||s.totalTargets!==1983||s.muRaw!==1913||s.muPublishable!==1913||s.unresolved!==70||s.muExcludedByCrossResultCollision!==0||s.duplicatePositiveSourceGroups!==0||s.duplicatePositiveReaderGroups!==0||s.duplicatePositiveDrnGroups!==0)throw new Error('Resultado DRM incompatible.');
if(!Array.isArray(research.results)||research.results.length!==1983)throw new Error('Resultados incompletos.');
const publish=research.results.filter(r=>r.publishable===true);if(publish.length!==1913)throw new Error(`Publishable=${publish.length}`);
const byId=new Map(pack.entries.map((r,i)=>[Number(r[0]),i])),terminalSource=new Map(),terminalReader=new Map(),terminalDrn=new Map();for(const r of pack.entries){if(![1,3,5].includes(Number(r[3])))continue;const id=Number(r[0]),sid=Number(r[1])||0,rid=Number(r[2])||0,drn=str(r[5]).toLowerCase();for(const [m,v] of [[terminalSource,sid],[terminalReader,rid],[terminalDrn,DRN_RE.test(drn)?drn:'']])if(v){const a=m.get(v)||[];a.push(id);m.set(v,a)}}
const before=pack.entries.map(r=>JSON.stringify(r)),beforeFallback=JSON.stringify(pack.readerFallbackGcdIds||[]),beforeFallbackReady=Number(pack.readerFallbackReady)||0,beforeLinkReady=Number(pack.linkReady)||0,changed=[];
for(const r of publish){const id=Number(r.gcdId),sid=Number(r.sourceId),rid=Number(r.readerId),drn=str(r.drn).toLowerCase();if(r.kind!=='mu'||r.functional!==true||r.landingUnlimited!==true||r.landingOpenButton!==true||!sid||!rid||!DRN_RE.test(drn)||!(Number(r.smartStatus)>=200&&Number(r.smartStatus)<400))throw new Error(`MU inseguro ${id}`);const pos=byId.get(id);if(pos==null||Number(pack.entries[pos][3])!==4)throw new Error(`GCD ${id} dejó status4`);for(const [label,m,v] of [['source',terminalSource,sid],['reader',terminalReader,rid],['drn',terminalDrn,drn]]){const owners=(m.get(v)||[]).filter(x=>x!==id);if(owners.length)throw new Error(`${label} collision ${id}: ${owners}`)}const cur=pack.entries[pos];pack.entries[pos]=[id,sid,rid,1,str(cur[4]),drn];changed.push(id)}
if(changed.length!==1913||new Set(changed).size!==1913)throw new Error('Scope de publicación incorrecto.');const changedSet=new Set(changed);for(let i=0;i<pack.entries.length;i++)if(!changedSet.has(Number(pack.entries[i][0]))&&JSON.stringify(pack.entries[i])!==before[i])throw new Error(`Regresión fuera de scope ${pack.entries[i][0]}`);if(new Set(pack.entries.map(r=>Number(r[0]))).size!==51002)throw new Error('GCD duplicados/perdidos.');
recompute(pack);if(pack.matched!==31102||pack.noDigital!==1135||pack.notListed!==18765||pack.linkReady!==beforeLinkReady+1913||pack.readerFallbackReady!==beforeFallbackReady||JSON.stringify(pack.readerFallbackGcdIds||[])!==beforeFallback||pack.functionalLinkReady!==31102||pack.functionalLinkMissing!==0)throw new Error(`Final inesperado ${pack.matched}/${pack.noDigital}/${pack.notListed}/${pack.linkReady}/${pack.readerFallbackReady}/${pack.functionalLinkMissing}`);
pack.generatedAt=now;pack.cleanCandidateDrmPublication={version:4,publishedAt:now,totalAudited:1983,promotedMU:1913,promotedUuid:1913,remainingUnresolvedFromCleanPass:70};pack.functionalLinkAudit={...(pack.functionalLinkAudit||{}),completedAt:now,uuidReady:pack.linkReady,readerFallbackReady:pack.readerFallbackReady,functionalLinkReady:pack.functionalLinkReady,functionalLinkMissing:pack.functionalLinkMissing};
const summary={version:4,publishedAt:now,changedRows:1913,promotedMU:1913,promotedUuid:1913,confirmedNoDigital:0,remainingUnresolvedFromPass:70,unchangedRows:51002-1913,after:{matched:pack.matched,noDigital:pack.noDigital,notListed:pack.notListed,linkReady:pack.linkReady,linkMissing:pack.linkMissing,readerFallbackReady:pack.readerFallbackReady,functionalLinkReady:pack.functionalLinkReady,functionalLinkMissing:pack.functionalLinkMissing}};await fs.writeFile(cacheFile,JSON.stringify(pack));await fs.writeFile(summaryFile,JSON.stringify(summary,null,2)+'\n');console.log(JSON.stringify(summary,null,2));
