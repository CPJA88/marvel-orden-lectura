import baseWorker from './worker-catalog-v11.js';

const SHARE='https://share.marvel.com';
const LEGACY='https://share.marvel.com/sharing/legacy/';

function unescapeHtml(v=''){return String(v).replace(/\\u002F/gi,'/').replace(/\\u003A/gi,':').replace(/\\\//g,'/').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'")}
async function probe(name,url,{redirect='follow',body=true}={}){const start=Date.now();try{const r=await fetch(url,{redirect,headers:{'User-Agent':'Mozilla/5.0 (compatible; MarvelLectura-Diagnostic/6.1)','Accept':'text/html,application/xhtml+xml,*/*;q=0.8'}}),text=body?await r.text():'';return{name,url,status:r.status,ok:r.ok,ms:Date.now()-start,finalUrl:r.url||'',location:r.headers.get('Location')||'',error:'',_text:text}}catch(e){return{name,url,status:0,ok:false,ms:Date.now()-start,finalUrl:'',location:'',error:String(e?.message||e),_text:''}}}
function publicProbe(p){const {_text,...x}=p;return x}
function pageSignals(html=''){const clean=unescapeHtml(html),readerIds=[...clean.matchAll(/https:\/\/read\.marvel\.com\/#\/book\/(\d+)/gi)].map(m=>m[1]),drns=[...clean.matchAll(/drn:src:marvel:unison::prod:[0-9a-f-]{36}/gi)].map(m=>m[0]);let title='',cover='';for(const re of [/<meta[^>]+property=["']og:title["'][^>]+content=["']([^"']+)["']/i,/<title[^>]*>([^<]+)<\/title>/i]){const m=clean.match(re);if(m){title=m[1].replace(/\s+/g,' ').trim();break}}for(const re of [/<meta[^>]+property=["']og:image["'][^>]+content=["']([^"']+)["']/i,/"image_url"\s*:\s*"([^"]+)"/i]){const m=clean.match(re);if(m){cover=unescapeHtml(m[1]);break}}return{pageTitle:title,readerIds:[...new Set(readerIds)].slice(0,5),drns:[...new Set(drns)].slice(0,5),coverFound:Boolean(cover),coverUrl:cover}}
function classify(f){if(f?.smartLink)return'OK';if(!f?.issueUrl){if(f?.searchReason&&f.searchReason!=='ok')return f.resolverSource==='marvel-search-error'?'MARVEL_SEARCH_HTTP_ERROR':'MARVEL_SEARCH_NO_EXACT_CANDIDATE';if(f?.catalogReason==='series-not-found')return'CATALOG_SERIES_NOT_FOUND';if(f?.catalogReason==='issue-not-in-series-map')return'CATALOG_ISSUE_NOT_IN_VISIBLE_MAP';return'LOOKUP_UNRESOLVED'}if(f?.reason==='reader-unavailable')return'CONFIRMED_NOT_IN_UNLIMITED';if(!f?.readerId)return'READER_ID_NOT_FOUND';if(!f?.drn)return'DRN_NOT_FOUND';return f?.reason||'UNKNOWN'}
async function traceMarvel(request,env){const original=new URL(request.url),title=(original.searchParams.get('title')||'').trim(),issue=(original.searchParams.get('issue')||'').trim(),year=(original.searchParams.get('year')||'').trim();if(!title)return Response.json({error:'missing-title'},{status:400});const diagUrl=new URL(original);diagUrl.pathname='/api/marvel/open';diagUrl.searchParams.set('mode','diagnostic');const start=Date.now();let f={};try{const r=await baseWorker.fetch(new Request(diagUrl.toString(),{headers:{Accept:'application/json'}}),env);f=await r.json();f._httpStatus=r.status}catch(e){f={diagnosticCode:'RESOLVER_ERROR',reason:'trace-base-error',error:String(e?.message||e)}}const attempts=[];if(!f.issueUrl&&f.searchUrl)attempts.push(publicProbe(await probe('marvel-search-exact',f.searchUrl)));else if(!f.issueUrl&&f.seriesUrl)attempts.push(publicProbe(await probe('series-page',f.seriesUrl)));let issueProbe=null,drnProbe=null,smartProbe=null;if(f.issueUrl){const p=await probe('issue-page',`${SHARE}${new URL(f.issueUrl).pathname}`);const sig=pageSignals(p._text);issueProbe={...publicProbe(p),...sig};const rid=f.readerId||sig.readerIds?.[0]||'';if(rid&&!f.drn){const d=await probe('legacy-drn',`${LEGACY}${encodeURIComponent(rid)}`);drnProbe={...publicProbe(d),...pageSignals(d._text)}}}if(f.smartLink)smartProbe=publicProbe(await probe('smart-link',f.smartLink,{redirect:'manual',body:false}));return Response.json({traceVersion:4,generatedAt:new Date().toISOString(),query:{title,issue,year},failureStage:classify(f),finalMs:Date.now()-start,final:{diagnosticCode:f.diagnosticCode||'',reason:f.reason||'',resolverVersion:f.resolverVersion||0,resolverSource:f.resolverSource||'',catalogReason:f.catalogReason||'',seriesTitle:f.seriesTitle||'',seriesLabel:f.seriesLabel||'',seriesUrl:f.seriesUrl||'',catalogKnownIssues:f.catalogKnownIssues||0,catalogKeys:f.catalogKeys||[],searchReason:f.searchReason||'',searchUrl:f.searchUrl||'',searchCandidateCount:f.searchCandidateCount||0,searchExactCount:f.searchExactCount||0,issueUrl:f.issueUrl||'',sourceId:f.sourceId||'',readerId:f.readerId||'',drn:f.drn||'',smartLink:f.smartLink||'',webUrl:f.webUrl||'',coverUrl:f.coverUrl||'',pageTitle:f.pageTitle||'',appStatus:f.appCheck?.status??0,webStatus:f.webCheck?.status??0,error:f.error||''},attempts,issueProbe,drnProbe,smartProbe},{headers:{'Cache-Control':'no-store'}})}
export default{async fetch(request,env,ctx){const url=new URL(request.url);if(url.pathname==='/api/marvel/trace')return traceMarvel(request,env);return baseWorker.fetch(request,env,ctx)}};